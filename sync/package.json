{
  "name": "carechoice-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Integration/sync service + PoC: Salesforce (integration user) <-> Supabase",
  "type": "module",
  "scripts": {
    "poc": "tsx --env-file-if-exists=.env src/poc.ts",
    "sync:read": "tsx --env-file-if-exists=.env src/syncRead.ts",
    "drain": "tsx --env-file-if-exists=.env src/drainOutbox.ts",
    "write:test": "tsx --env-file-if-exists=.env src/writeTestCaseNote.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "jsforce": "^3.3.0"
  },
  "devDependencies": {
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
